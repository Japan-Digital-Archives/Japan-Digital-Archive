{% form_theme form theme %}

<form action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="fos_user_change_password">
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-info" type="submit" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}" /> <a id="close-button" class="btn" href="#" onclick="javascript: parent.$('#user-modal').trigger('close');">{% trans %}jda.close{% endtrans %}</a>	
    </div>
</form>
